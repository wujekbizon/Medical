<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:Medical.ViewModels"
                    xmlns:vw="clr-namespace:Medical.Views"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
                    >

    <DataTemplate DataType="{x:Type vm:NowaFakturaViewModel}">
        <vw:NowaFakturaView />
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowaHistoriaNaprawViewModel}">
        <vw:NowaHistoraNaprawView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowaKaretkaViewModel}">
        <vw:NowaKaretkaView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowaOceanZespoluViewModel}">
        <vw:NowaOcenaZespoluView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowaPlacowkaViewModel}">
        <vw:NowaPlacowkaView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowaPozycjaFakturyViewModel}">
        <vw:NowaPozycjaFakturyView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowaRolaPracownikaViewModel}">
        <vw:NowaRolaPracownikaView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowaUdzielonaPomocViewModel}">
        <vw:NowaUdzielonaPomocView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NoweKosztyUtrzymaniaViewModel}">
        <vw:NowyKosztUtrzymaniaView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NoweZlecenieWyjazduViewModel}">
        <vw:NoweZlecenieWyjazduView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowyKontrahentViewModel}">
        <vw:NowyKontrahentView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowyPacjentViewModel}">
        <vw:NowyPacjentView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowyPracownikViewModel}">
        <vw:NowyPracownikView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowySposobPlatnosciViewModel}">
        <vw:NowySposobPlatnosciView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowyZespolPracownikViewModel}">
        <vw:NowyZespolPracownikView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowyZespolRatunkowyViewModel}">
        <vw:NowyZespolRatunkowyView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszyscyKontrahenciViewModel}">
        <vw:WszyscyKontrahenciView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszyscyPacjenciViewModel}">
        <vw:WszyscyPacjenciView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszyscyPracownicyViewModel}">
        <vw:WszyscyPracownicyView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkieFakturyViewModel}">
        <vw:WszystkieFakturyView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkieHistorieNaprawViewModel}">
        <vw:WszystkieHistorieNaprawView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkieKaretkiViewModel}">
        <vw:WszystkieKaretkiView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkieKosztyUtrzymaniaViewModel}">
        <vw:WszystkieKosztyUtrzymaniaView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkieOcenyZespoluViewModel}">
        <vw:WszystkieOcenyZespoluView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkiePlacowkiViewModel}">
        <vw:WszystkiePlacowkiView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkiePozycjeFakturyViewModel}">
        <vw:WszystkiePozycjeFakturyView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkieRolePracownikaViewModel}">
        <vw:WszystkieRolePracownikaView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkieSposobyPlatnosciViewModel}">
        <vw:WszystkieSposobuPlatnosciView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkieUdzielonePomoceViewModel}">
        <vw:WszystkieUdzielonePomoceView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkieZespolPracownikViewModel}">
        <vw:WszystkieZespolPracownikView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkieZespolyRatunkoweViewModel}">
        <vw:WszystkieZespolyRatunkoweView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:WszystkieZleceniaWyjazduViewModel}">
        <vw:WszystkieZleceniaWyjazduView/>
    </DataTemplate>

    <Style x:Key="MainHCCStyle" TargetType="{x:Type HeaderedContentControl}">
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Border
                        Background="{DynamicResource MaterialDesignPaper}"
                        BorderBrush="{DynamicResource BorderDark}"
                        BorderThickness="0,0,0,2"
                        CornerRadius="0"
                        Margin="0"
                        Padding="16,12"
                        SnapsToDevicePixels="True">
                        <TextBlock
                            FontSize="16"
                            FontWeight="Medium"
                            Foreground="{DynamicResource MaterialDesignBody}"
                            Text="{TemplateBinding Content}"
                            VerticalAlignment="Center"/>
                    </Border>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type HeaderedContentControl}">
                    <DockPanel>
                        <ContentPresenter
                            DockPanel.Dock="Top"
                            ContentSource="Header"
                            ContentTemplate="{TemplateBinding HeaderTemplate}" />
                        <ContentPresenter
                            ContentSource="Content"
                            ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MainBorderStyle" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{DynamicResource DarkSurface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderDark}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <DataTemplate x:Key="CommandsTemplate">
        <ItemsControl ItemsSource="{Binding}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Border Background="{DynamicResource DarkSurfaceVariant}"
                            BorderBrush="{DynamicResource AccentPurple}"
                            BorderThickness="0,0,0,2"
                            Padding="12,8"
                            Margin="0,12,0,8"
                            CornerRadius="4,4,0,0"
                            Visibility="{Binding ShowSectionHeader, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="{Binding SectionName}"
                                   FontSize="11"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource AccentPurple}"/>
                        </Border>
                        <ItemsControl ItemsSource="{Binding Commands}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <materialDesign:Card 
                                    Margin="4,2"
                                    UniformCornerRadius="6"
                                    materialDesign:ElevationAssist.Elevation="Dp0">

                                        <Border 
                                        x:Name="CommandBorder"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource BorderDark}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Cursor="Hand">
                                            <materialDesign:Ripple
                                            Feedback="{DynamicResource TextPrimary}"
                                            Padding="12,10"
                                            HorizontalContentAlignment="Stretch">
                                                <materialDesign:Ripple.InputBindings>
                                                    <MouseBinding MouseAction="LeftClick" Command="{Binding Command}"/>
                                                </materialDesign:Ripple.InputBindings>

                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <materialIcons:MaterialIcon 
                                                    x:Name="CommandIcon"
                                                    Kind="{Binding IconKind}"
                                                    Width="20"
                                                    Height="20"
                                                    Grid.Column="0"
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,12,0"
                                                    Foreground="{DynamicResource AccentPurple}">
                                                        <materialIcons:MaterialIcon.RenderTransform>
                                                            <ScaleTransform ScaleX="1" ScaleY="1" CenterX="10" CenterY="10"/>
                                                        </materialIcons:MaterialIcon.RenderTransform>
                                                    </materialIcons:MaterialIcon>
                                                    <TextBlock 
                                                    x:Name="CommandText"
                                                    Grid.Column="1"
                                                    Text="{Binding DisplayName}"
                                                    Foreground="{DynamicResource TextPrimary}"
                                                    FontSize="14"
                                                    VerticalAlignment="Center"
                                                    TextWrapping="NoWrap"/>
                                                </Grid>
                                            </materialDesign:Ripple>
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{DynamicResource DarkSurfaceVariant}"/>
                                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentPurple}"/>
                                                            <Setter Property="Effect">
                                                                <Setter.Value>
                                                                    <DropShadowEffect 
                                                                    Color="#7B68EE" 
                                                                    BlurRadius="10" 
                                                                    ShadowDepth="0" 
                                                                    Opacity="0.5"/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </materialDesign:Card>
                                    <DataTemplate.Triggers>
                                        <Trigger SourceName="CommandBorder" Property="IsMouseOver" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation 
                                                        Storyboard.TargetName="CommandIcon"
                                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                        To="1.2"
                                                        Duration="0:0:0.15"/>
                                                        <DoubleAnimation 
                                                        Storyboard.TargetName="CommandIcon"
                                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                        To="1.2"
                                                        Duration="0:0:0.15"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation 
                                                        Storyboard.TargetName="CommandIcon"
                                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                        To="1.0"
                                                        Duration="0:0:0.15"/>
                                                        <DoubleAnimation 
                                                        Storyboard.TargetName="CommandIcon"
                                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                        To="1.0"
                                                        Duration="0:0:0.15"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DataTemplate>
    <DataTemplate x:Key="ClosableTabItemTemplate">
        <DockPanel MinWidth="120" Background="Transparent">
            <Button
                Command="{Binding Path=CloseCommand}"
                DockPanel.Dock="Right"
                Margin="8,0,0,0"
                Padding="0"
                Width="20"
                Height="20"
                Style="{DynamicResource MaterialDesignToolButton}"
                ToolTip="Zamknij zakładkę">
                <materialIcons:MaterialIcon
                    Kind="Close"
                    Width="16"
                    Height="16"/>
            </Button>
            <TextBlock
                Text="{Binding Path=DisplayName}"
                VerticalAlignment="Center"
                Margin="8,0,0,0"
                FontSize="13"
                FontWeight="Normal"
                Foreground="{DynamicResource MaterialDesignBody}"/>
        </DockPanel>
    </DataTemplate>
    <DataTemplate x:Key="WorkspacesTemplate">
        <TabControl
            IsSynchronizedWithCurrentItem="True"
            ItemsSource="{Binding}"
            ItemTemplate="{StaticResource ClosableTabItemTemplate}"
            Margin="0"
            Background="{DynamicResource MaterialDesignPaper}"
            Style="{DynamicResource MaterialDesignTabControl}">
        </TabControl>
    </DataTemplate>
</ResourceDictionary>