<local:WszystkieViewBase x:Class="Medical.Views.WszystkieFakturyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Medical.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#252526">
        <Grid.RowDefinitions>
            <RowDefinition Height="0*"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <DataGrid AutoGenerateColumns="False" Margin="0,-2,0,0" Grid.RowSpan="2" ItemsSource="{Binding List}" SelectedItem="{Binding WybranaFaktura, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Numer" Binding="{Binding Numer}" />
                <DataGridTextColumn Header="Data Wystawienia" Binding="{Binding DataWystawienia, StringFormat={}{0:dd.MM.yyyy}}" />
                <DataGridTextColumn Header="Termin Płatności" Binding="{Binding TerminPlatnosci, StringFormat={}{0:dd.MM.yyyy}}" />
                <DataGridTextColumn Header="Waluta" Binding="{Binding Waluta}" />
                <DataGridTextColumn Header="Status Płatności" Binding="{Binding StatusPlatnosci}" />
                <DataGridTextColumn Header="Opis" Binding="{Binding Opis}" />
                <DataGridTextColumn Header="Kategoria Kosztu" Binding="{Binding KategoriaKosztu}" />
                <DataGridTextColumn Header="Okres Księgowy" Binding="{Binding OkresKsiegowy}" />
                <DataGridTextColumn Header="Zatwierdzona" Binding="{Binding CzyZatwierdzona}" />
                <DataGridTextColumn Header="Nazwa Firmy" Binding="{Binding NazwaFirmy}" />
                <DataGridTextColumn Header="Sposób Płatności" Binding="{Binding NazwaSposobuPlatnosci}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>