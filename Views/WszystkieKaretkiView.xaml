<local:WszystkieViewBase x:Class="Medical.Views.WszystkieKaretkiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Medical.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#252526">
        <Grid.RowDefinitions>
            <RowDefinition Height="0*"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <DataGrid AutoGenerateColumns="False" Margin="0,-2,0,0" Grid.RowSpan="2" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdKaretki}" />
                <DataGridTextColumn Header="Numer Rejestracyjny" Binding="{Binding NumerRejestracyjny}" />
                <DataGridTextColumn Header="Typ" Binding="{Binding TypKaretki}" />
                <DataGridTextColumn Header="Rok" Binding="{Binding RokProdukcji}" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" />
                <DataGridTextColumn Header="Przebieg" Binding="{Binding PrzebiegKm}" />
                <DataGridTextColumn Header="Data Ostatniego Przeglądu" Binding="{Binding DataOstatniegoPrzegladu}" />
                <DataGridTextColumn Header="Ważność Ubezpieczenia" Binding="{Binding DataWaznosciUbezpieczenia}" />
                <DataGridTextColumn Header="Pojemność silnika" Binding="{Binding PojemnoscSilnika}" />
                <DataGridTextColumn Header="MocKM" Binding="{Binding MocSilnikaKM}" />
                <DataGridTextColumn Header="VIN" Binding="{Binding NumerVIN}" />
                <DataGridTextColumn Header="Rodzaj Paliwa" Binding="{Binding TypPaliwa}" />
                <DataGridTextColumn Header="Kolor" Binding="{Binding Kolor}" />
                <DataGridTextColumn Header="Liczba Miejsc" Binding="{Binding LiczbaMiejsc}" />
                <DataGridTextColumn Header="Data Nabycia" Binding="{Binding DataNabycia}" />
                <DataGridTextColumn Header="Żywotność" Binding="{Binding SzacowanaZywotnoscLat}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
