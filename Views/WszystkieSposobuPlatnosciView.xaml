<local:WszystkieViewBase x:Class="Medical.Views.WszystkieSposobuPlatnosciView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Medical.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#252526">
        <Grid.RowDefinitions>
            <RowDefinition Height="0*"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <DataGrid AutoGenerateColumns="False" Margin="0,-2,0,0" Grid.RowSpan="2" ItemsSource="{Binding List}" SelectedItem="{Binding WybranySposobPlatnosci, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nazwa Płatności" Binding="{Binding Nazwa}" />
                <DataGridTextColumn Header="Opis" Binding="{Binding Opis}" />
                <DataGridTextColumn Header="Wymagane potwierdzenie" Binding="{Binding CzyWymagaPotwierdzenia}" />
                <DataGridTextColumn Header="Rodzaj Transakcji" Binding="{Binding RodzajTransakcji}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
