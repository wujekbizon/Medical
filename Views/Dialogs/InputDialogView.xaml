<Window x:Class="Medical.Views.Dialogs.InputDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Medical.Views.Dialogs"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:dialogs="clr-namespace:Medical.ViewModels.Dialogs"
        mc:Ignorable="d"
        Title="InputDialogView" Height="450" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    

        <materialDesign:Card Style="{StaticResource CardStyle}">
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" 
                        Orientation="Horizontal" 
                        Margin="0,0,0,20">
                <Border Background="{DynamicResource AccentPurple}" 
                        Width="4" 
                        Height="20" 
                        CornerRadius="2" 
                        Margin="0,0,12,0"
                        VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Title}" 
                           FontSize="18"
                           FontWeight="Light"
                           Foreground="White"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Grid.Row="1" 
                       Text="{Binding Prompt}" 
                       FontSize="13"
                       FontWeight="Light"
                       Foreground="#CCFFFFFF"
                       Margin="0,0,0,16"
                       TextWrapping="Wrap"/>

            <TextBox Grid.Row="2" 
                     Text="{Binding ResponseText, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Wpisz tekst..."
                     BorderBrush="#44FFFFFF"
                     Foreground="White"
                     FontSize="15"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     VerticalAlignment="Top"
                     Margin="0,0,0,20"/>

            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="Anuluj" 
                        Width="100"
                        Height="36"
                        Margin="0,0,12,0"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        BorderBrush="#44FFFFFF"
                        Foreground="White"/>
                <Button Content="OK" 
                        Width="100"
                        Height="36"
                        Command="{Binding OkCommand}"
                        IsEnabled="{Binding CzyMoznaZatwierdzic}"
                        Background="{DynamicResource AccentPurple}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                            <Setter Property="Background" Value="{DynamicResource AccentPurple}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>
